# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# production
/build

# env — NEVER commit these (§49)
.env
.env.local
.env*.local
.env.development
.env.production

# vercel
.vercel

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Working brief — contains a live MongoDB credential at §1 (see
# docs/phase-1-assessment.md §0). Ignored so it cannot be committed.
# Once the credential is rotated AND redacted from this file, it is safe to
# remove this rule and commit the brief as project documentation.
master-prompt.md
